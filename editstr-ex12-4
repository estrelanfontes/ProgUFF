#include <stdio.h>
#define MAX 20

int main() {
    char c;
    int i;
    char str[] = {"estrela"};
    printf("digite um caractere: ");
    scanf("%c",&c);
    for (i=0;i<MAX;i++){
        if (str[i]!=c && str[i]!='\0') printf("%c",str[i]);
        
    }

    return 0;
}
