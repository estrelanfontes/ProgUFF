#include <stdio.h>

float media(int n, float *v){
int i, soma = 0;
for (i=0;i<=n;i++){
soma += v[i];    
}
return (soma/(float)n); //conversão de tipo
}

int main(){
int n = 4;
float m;
float v[] = {1,2,3,4};
m = media(n,v); //passagem de vetores entre funções
printf("%f", m);    
return 0;
}
